try:
    contenido=urlib.request.urlopen("http://..:"
except urllib.error.urlError:
    print ("url incorrecta"

#####
try:
    archivo=open("farmacias.dat)
except FileNotFoundError:
                 print("El Archivo no se encuentra")
