def traductor(cadena,dicc):
    palabras = cadenas.split()

    for p in range (len(palabras)):
        if palabra[i] in dicc:
            palabras[i] = dicc[palabras[i]]
    return " ".join(palabras)

